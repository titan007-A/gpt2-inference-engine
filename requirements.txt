# GPT-2 Optimized Inference Engine - dependencies
# Install:  pip install -r requirements.txt
#
# The plain `torch` below is the CPU build. For an NVIDIA GPU, install a CUDA build
# from the official index instead, matching the cuXXX tag to your driver, e.g.:
#   pip install --upgrade torch --index-url https://download.pytorch.org/whl/cu130
# RTX 50-series (Blackwell / sm_120) needs cu128 or newer; this repo was set up with
# torch 2.14.0+cu130 on an RTX 5050 (driver CUDA 13.2).

# --- core ---
torch>=2.2
transformers>=4.40
tokenizers>=0.19
numpy>=1.26
pyyaml>=6.0

# --- serving (Phase 7) ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
sse-starlette>=2.1

# --- quantization (Phase 5, optional) ---
# bitsandbytes>=0.43        # Windows may need a prebuilt wheel; enable when you reach Phase 5

# --- benchmarking / profiling (Phase 8) ---
rich>=13.7

# --- dev / tests ---
pytest>=8.1
